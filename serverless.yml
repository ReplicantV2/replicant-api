org: udittlamba
app: replicant-api
service: replicant-api
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs16.x

functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: 'ANY /{proxy+}'

plugins:
  - serverless-plugin-typescript
